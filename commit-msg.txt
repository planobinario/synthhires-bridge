feat(daemon): desktop.fs.verify — empirical path access verification

- fs_ops::verify: reports exists/is_dir/readable/writable measured on
  the real machine; the writable axis creates a unique probe file,
  reads it back byte-for-byte, and removes it — user files untouched
- ws_client: desktop.fs.verify arm bypasses path gating (the path is
  not yet allowed — that's the whole point of verifying) and returns
  the full per-axis report
- deep-link pairing desired_scopes includes desktop.fs.verify
- Version bump 0.1.16

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
